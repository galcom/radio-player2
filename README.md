This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## for dev mode:
`yarn dev-start`

#run server:
`node server.js`


### azure deployment
`deploy_az_container.sh radio-player 1.1.1`
On kubernetes server, edit version in radio-player.yaml and apply
